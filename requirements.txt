# Core
pandas>=2.0.0
requests>=2.31.0

# Geospatial (minimal)
shapely>=2.0.0  # For basic distance/buffer calculations
geopandas>=0.13.0  # Optional (only if you need spatial joins later)

# Notebooks (optional)
jupyter>=1.0.0  # For /notebooks exploration

# API/Scraping (if needed)
sodapy>=1.5.0  # For Chicago Data Portal API (alternative to requests)